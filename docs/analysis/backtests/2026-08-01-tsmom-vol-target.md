# H-012 — 변동성 타깃 시계열 모멘텀 (랭킹 없는 breadth 방식)

작성 2026-08-01 · 상태: **완료 · 판정 기각 (2차 시도)** · 시장 KR + US

> §1~§4는 **결과를 보기 전에** 작성·고정했다. 사전 판정 기준(§3)은 실행 후 변경하지
> 않는다. (PROTOCOL §3)
>
> **이 문서는 1차 시도에서 게이트 3회를 소진해 한 번 기각됐다.** 당시 체크리스트
> 1·2·3(중복·구현가능·조건부합)은 3회 모두 통과했고 4·5(사전 기준·무모호성)에서만
> 걸렸다 — **가설이 아니라 명세 정밀도의 실패**였다. 그 경험을 근거로 PROTOCOL §4-(a)에
> ① 1·2·3 통과 시 게이트 조건부 5회 연장 ② **수식·산식 검증 항목(6번)** 을 추가했고,
> 아래 5건을 고쳐 2차 시도로 재개한다.
>
> | # | 1차 최종 지적 | 이 문서의 수정 |
> |---|---|---|
> | ① | Memmel CI의 SE 스케일 누락 (CI 폭이 실제의 1/400) | §3에서 스케일된 `Var(ΔSR)` 식으로 교체 + **차원·극단값 검증 표** 첨부 |
> | ② | 재배분 예산이 `1−E` 버퍼까지 소진 (조건 ③의 처치 소멸) | 예산을 `기준자산×E − 재배분 직전 보유 평가액`으로 정정 + **소형 수치 예제** 첨부 |
> | ③ | 재배분 루프가 비결정적 | §2.3 전체를 재작성 — 집행 순서 키를 전 단계 공통으로 뽑고, 목표 수량 `q_i`를 2단계에서 확정, 재배분은 **종목당 최대 1주·1라운드**로 종결 |
> | ④ | "투자일" 정의가 2해석 | **리밸→리밸 보유 블록** 기준으로 확정(달력 월로 라벨링하면 월말 리밸 스케줄과 한 달 어긋난다) |
> | ⑤ | §3에 폐기된 정의의 수치 잔존 | 삭제하고 "채택 정의로는 미측정"으로 대체 |
>
> 2차 시도 1회차에서 추가로 지적된 5건도 반영했다: 재배분이 "1주 더하면 목표 초과 →
> 건너뜀" 규칙 때문에 **영구 no-op**이 되던 문제(개별 목표 초과를 1주까지 허용),
> 투자일 라벨링의 오프바이원(위 ④), ③의 CI 기준을 **`E<1` 달 CI**로 확정,
> §2.3-5의 동점 정의를 목표 비중 기준으로 통일, 재배분 예산에서 좀비 이중 차감 제거.

---

## 1. 가설

**종목을 서로 비교해 순위를 매기지 말고, 각 종목이 "자기 기준으로 오르고 있는가"만
보고 담되, 변동성이 큰 종목은 적게 담고 포트폴리오 전체 변동성을 일정하게 유지하면
벤치마크를 이긴다.**

출처: Moskowitz, Ooi & Pedersen, "Time Series Momentum", *JFE* 104(2), 2012
(절대 모멘텀 + 역변동성 가중) · Barroso & Santa-Clara, "Momentum has its moments",
*JFE* 116(1), 2015 (변동성 타깃이 모멘텀 붕괴를 제거하고 Sharpe를 거의 두 배로 올린다).

### H-009·H-010과 무엇이 다른가

앞의 두 테스트는 **"90종목 중 20개를 어떻게 고를까"**를 물었고 둘 다 기각됐다.
H-012는 **고르지 않는다.** 조건을 만족하는 종목을 전부 담고, 대신 **얼마나 담을지**로
승부한다. 보유 종목 수가 시장 상태에 따라 저절로 변한다(강세장 후반엔 늘고 약세
전환 초입엔 준다).

| 축 | H-009 | H-010 | **H-012** |
|---|---|---|---|
| 종목 선택 | 상대 랭킹 상위 20% | 상대 랭킹 20종목 | **랭킹 없음 — 조건 충족 전부** |
| 보유 종목수 | 고정(20) | 고정(20) | **가변** |
| 비중 | ATR 역비례 | 동일비중 | **60일 역변동성 비례** |
| 총 익스포저 | 현금 한도 | 100% | **변동성 타깃으로 동적 조절** |
| 리밸런싱 | 주간 | 월간 | 월간 |

가설 키:
```
진입: 252일 수익률 > 0 인 유동성 유니버스 전 종목,
      60일 역변동성 가중 + 연 12% 변동성 타깃(126 투자일 실현변동성), 월간 리밸런싱
매도: 252일 수익률 ≤ 0 종목 청산
레짐: 지수 ≤ SMA200이면 전량 현금
시장: KR · US
```

## 2. 규칙 명세 (일봉 종가 기준)

### 2.1 지표 — 판단일 t의 종가까지만 사용

| 지표 | 정의 |
|---|---|
| **절대 모멘텀** | `close[t] / close[t−252] − 1 > 0` |
| **σ60** | 직전 **60거래일** 일간수익률 표준편차(ddof=1). 비중 계산용 |
| **포트폴리오 실현변동성** | **무스케일(unscaled) 계열** — 익스포저를 적용하지 않은 계열(= 그 그룹의 `E=1` 쌍둥이)의 **직전 126 "투자일"** 일간수익률 표준편차(ddof=1) × √252. **투자일 = "직전 리밸런싱일의 레짐 플래그가 강세였던 보유 블록"에 속한 모든 거래일**이다. 보유 블록 = `그 리밸일 다음 거래일 ~ 다음 리밸일 종가`. **달력 월이 아니라 리밸→리밸 블록으로 정의**한다 — 리밸일이 월말이라 7월 말 판정이 지배하는 구간은 8월이므로, "그 달 리밸일 플래그로 그 달을 라벨링"하면 **한 달 어긋난다**. 그렇게 하면 강세로 진입해 보유 중 급락하고 월말에 약세로 뒤집힌 달이 통째로 창에서 빠져, **위험이 커진 바로 그 구간을 빼고 변동성을 낮게 재는** 역효과가 난다. 일별 레짐 플래그로도 판정하지 않고(같은 이유), 수익률 0으로 역추정하지도 않는다(좀비 동결 등으로 비현금일에도 0이 나올 수 있다). 창은 판단일 t를 **제외**하고 t−1부터 과거로 세어 투자일 126개를 채운다 |
| **레짐** | 벤치마크 종가 > 벤치마크 SMA200 (PROTOCOL §2 고정값) |

**결측·이상값 처리**

- **인덱싱 축**: 모든 `t−k`는 시장 공통 거래일 축의 위치이며, 데이터가 없는 날은
  NaN으로 남긴다(ffill 금지).
- 일간수익률은 **공통 거래일 축을 유지한 채 인접 두 봉으로** 계산한다
  (`ret[t] = close[t]/close[t−1] − 1`). NaN을 드롭한 뒤 `pct_change`를 쓰면 정지 구간을
  건너뛴 다일 수익률이 1일 수익률로 섞여 σ60이 부풀려진다. 마스킹된 봉은 인접 수익률
  2개를 NaN으로 만들며, 그것이 유효일 기준(60개 중 55개)에 그대로 반영된다.
- **종가 `또는` 시가가 0·음수이면 해당 봉만 NaN 처리**한다(OR 조건). KR 거래정지 봉은
  `open = high = low = 0`이지만 **종가는 직전가로 양수 이월**돼 있어, "둘 다 0"(AND)으로
  검사하면 9,008봉이 전부 통과한다 (PROTOCOL §1 · H-010 §5.5 P1).
  - 이 가설에서는 체결가 문제로 끝나지 않는다. 이월 종가는 **일간수익률 0을 대량
    생성**해 σ60을 인위적으로 낮추고, 비중이 `1/σ60`이라 **거래정지가 잦은 종목이 최대
    비중**을 받는다. 동시에 무스케일 계열의 126일 변동성도 낮아져 E가 과대 산출된다.
    **가설의 두 핵심 장치가 동시에 오염되므로 이 마스킹은 필수다.**
- `close[t−252]` 결측이면 그달 제외. σ60은 **종가 61개(`close[t−60]`~`close[t]`) 사이의
  일간수익률 60개**로 계산하며, 유효(비결측) 수익률일이 **55개 미만**이면 제외한다.
  판단일 t를 포함하는 것은 t 종가로 계산 가능하기 때문이고, 포트폴리오 변동성이 t를
  제외하는 것은 **그 값이 t의 익스포저를 정하는 입력**이라 t 수익률이 아직 정의되지
  않기 때문이다(비대칭에 근거가 있다).
- σ60 = 0(무거래로 가격 불변)인 종목은 역수가 발산하므로 제외한다.
  **이 판정은 매 리밸런싱일 t 시점의 σ60로만 한다** — 전 기간을 스캔해 종목을 배제하면
  미래의 무거래 구간으로 과거 유니버스를 거르는 look-ahead가 된다.

> **무스케일 정의를 쓰는 이유(P3 반영)** — "전략 자신의 수익률"로 변동성을 재면
> `E↓ → 측정 변동성↓ → 다음 달 E↑`의 되먹임이 생겨 처치가 스스로 희석된다(실측: 문서
> 초안식이 논문식보다 US +0.012·KR +0.044만큼 **덜 줄인다**). 무스케일 계열을 쓰면
> A는 **근사적으로** `E_t × D`가 되어 D와의 관계가 "익스포저 하나만 다른" 형태에
> 가까워진다(정수 내림·재배분·비용 차이 때문에 정확히 같지는 않다).
>
> **"투자일 126개"로 세는 이유** — 레짐 현금일의 0 수익률이 창을 채우면 변동성이
> 기계적으로 내려가 **약세장 직후 재진입 시점에 익스포저가 최대**가 되는 정반대 동작이
> 나온다. 그렇다고 "달력 126일 중 현금일 제외 + 유효일 부족 시 E=1" 방식으로 두면
> **3개월 이상 약세 뒤 재진입하는 바로 그 달에 폴백이 걸려 E=1**이 되어, 막으려던 동작이
> 그대로 재현된다. **투자일로 세면 폴백 자체가 불필요**하다(전략 시작 직후 워밍업만
> 예외 — 투자일 126개가 아직 없으면 `E = 1`).

### 2.2 유니버스 (PIT — 매 리밸런싱 시점 재계산)

H-009·H-010과 동일하게 맞춘다.

- **직전 60거래일(판단일 t 포함) 거래대금(종가×거래량) 중앙값** 상위
  **N = US 500 / KR 300**. PROTOCOL §2의 US 유동성 하한($2M)은 이 상위 N 규칙으로
  대체한다(H-009·H-010과 유니버스를 맞추기 위함).
- 개별 종목 최소 이력 **253봉**(`close[t−252]` 참조에 필요)
- 시가총액 스냅샷은 쓰지 않는다

**용어**

| 용어 | 정의 |
|---|---|
| **적격 종목** | 유동성 유니버스 중 `252일 수익률 > 0` ∧ σ60 유효(양수)인 종목 **전부**. 개수 제한 없음 |
| **원시 비중** | `w_i ∝ 1 / σ60_i`, 적격 종목 전체에 대해 합이 1이 되도록 정규화 |
| **익스포저** | `E = min(1, 0.12 / 포트폴리오 실현변동성)` — 레버리지 금지(1 초과 불가) |
| **목표 비중** | `w_i × E`. 나머지 `1 − E`는 현금 |

### 2.3 매매 (월간 리밸런싱 — 매월 마지막 거래일)

판단일 **종가로 체결**한다고 가정한다(§7에 명시, §5에서 익일 시가 체결 민감도 보고).

**집행 순서 키(전 단계 공통)**: **해당 그룹의 목표 비중 내림차순, 동점은 티커 사전순.**
A·D·E에서는 σ60 오름차순과 같고, 동일비중 C에서는 전부 동점이라 티커순이 된다.
**σ60을 순서 키로 쓰지 않는 이유**: σ60 순으로 돌리면 현금이 구속될 때 C에도 저변동성
종목만 먼저 채워져 **C가 부분적으로 역변동성 가중이 되어 조건 ②의 처치가 희석된다.**

1. **레짐 확인**: 벤치마크 종가 ≤ SMA200 → **전량 청산 후 현금**, 이번 달 매수 없음.
2. **목표 산출**: 적격 종목 전체에 대해 `목표금액_i = 기준자산 × w_i × E`.
   - 기준 자산 = 리밸런싱 직전 **(현금 + 보유 평가액)** 합계. **초기 자본 = 1e8**
     (H-009·H-010과 동일. 정수 내림 누수가 자본 규모에 의존하므로 명시한다.)
   - 투자일 126개가 아직 없으면 **E = 1**(워밍업)
   - **목표 수량** `q_i = floor(목표금액_i / 종가_i)`
3. **매도**: (a) 적격에서 빠진 보유 종목 **전량 매도** → (b) 보유 수량이 `q_i`를 넘는
   종목은 초과분 **부분 매도**. 매도를 모두 끝낸 뒤 4단계로 간다(매도 대금이 매수 재원).
4. **매수**: 보유 수량이 `q_i`에 미달인 종목을 **집행 순서 키대로** `q_i`까지 매수한다.
   비용 포함 소요액이 잔여 현금을 넘으면 그 종목은 건너뛰고 다음으로 진행한다.
5. **잔여 현금 재배분** — 정수 내림 누수만 되돌린다. `1 − E` 현금 버퍼는 건드리지 않는다.
   - **예산** = `기준자산 × E − (재배분 직전, **목표에 포함된 종목**의 보유 평가액)`.
     0 이하면 재배분하지 않는다.
     - **좀비 포지션은 예산 계산에서 제외**한다(7번). 좀비는 목표에 없으면서 보유
       평가액에는 잡히므로, 총 보유액을 그대로 빼면 예산이 좀비 크기만큼 과소 산출된다.
     - **이 산식이 정수 내림 누수와 같은 이유**: 목표 종목의 보유 평가액은 `기준자산 × E`를
       종목별로 정수 주 내림한 결과이므로, 차액이 곧 내림으로 버려진 금액이다.
       **`1 − E` 버퍼는 정의상 이 차액에 들어오지 않는다.**
     - **소형 예제 검증**: 기준자산 1,000만 · E = 0.6 → 목표 투자 600만. 정수 내림 결과
       목표 종목 보유가 588만이면 예산 = 600 − 588 = **12만**(누수분). 현금은 412만이지만
       그중 400만(= `1−E`)은 예산 밖이다.
   - **순회**: 목표에 포함된 종목 전체를 집행 순서 키대로 돌며 **1주씩 추가**한다.
     - **개별 목표 금액 초과를 허용한다**(누수 회수는 정의상 목표를 소폭 넘길 수밖에
       없다). 대신 **한 종목이 받을 수 있는 추가분은 라운드당 1주, 누적 최대 1주**로
       제한해 특정 종목에 쏠리지 않게 한다 → 실질적으로 **1라운드만 돈다.**
     - **한도**: 비용 포함 소요액이 **남은 예산 또는 잔여 현금**을 넘으면 그 종목은
       건너뛴다. 재배분 매수에도 §2.4 비용이 부과된다.
     - **종료**: 순회를 한 바퀴 마치면 종료한다(각 종목 최대 1주이므로 재순회 없음).
   - 부가 기록: **그룹별 "목표 익스포저 대비 실현 익스포저"**와 **목표 비중 대비 실현
     비중의 평균 절대 편차**.
   - **이 규칙이 필요한 이유**: KR은 고가주와 작은 원시비중이 겹쳐 정수 내림 누수가 크다.
     실측으로 `E=1` 지시 시 실제 투입 **91.0%**, `E=0.5` 지시 시 **83.4%**로 **E가
     작을수록 누수가 커진다.** 재배분 없이 두면 A(E<1)가 D(E=1)보다 구조적으로 더 새어,
     ③이 재려는 "익스포저 조절 효과"에 정수 내림 아티팩트가 상태 의존적으로 섞인다.
6. **좀비 포지션**: 가격이 끊긴 보유 종목은 마지막 유효가로 동결 평가하고, **유효가가
   없어 체결이 불가능한 동안은 보유를 유지**한다(3-(a)의 전량 매도 규칙에 대한 특칙 —
   팔 수가 없다). 비중 정규화 분모와 5번 예산에는 **포함하지 않는다**. 건수를 부가
   기록으로 보고한다.


### 2.4 비용

체결 **금액에 정률**로 편도 비용을 부과한다(매수·매도 각각, 부분 체결 포함).
PROTOCOL §2의 왕복 비용을 편도로 환산 — **US 0.05% / KR 0.14%**.

### 2.5 대조군 — A에서 **딱 한 요소만** 바꾼다

이 가설은 세 부품(절대 모멘텀 필터 · 역변동성 가중 · 변동성 타깃)의 조합이다.
각 부품을 하나씩 빼서 기여를 분리한다. 표에 적힌 것 외의 규칙은 A와 완전히 동일하다.

| 그룹 | A에서 바뀌는 것 | 무엇을 분리하는가 |
|---|---|---|
| **A** | — (세 부품 전부) | — |
| **B** | 전략 없이 벤치마크 매수 후 보유 | 절대 기준선 |
| **C** | **역변동성 가중 제거** → 적격 종목 **동일비중** | 역변동성 가중의 기여 |
| **D** | **변동성 타깃 제거** → 항상 `E = 1` | 익스포저 조절의 기여 |
| **E** | **절대 모멘텀 필터 제거** → 유동성 유니버스 **전 종목** 보유 | 모멘텀 틸트의 기여 |

**각 그룹의 익스포저 E는 자기 무스케일 계열로 계산한다.** A는 실제 그룹 D의 수익률을
그대로 쓰지만, **C와 E그룹은 자기 규칙의 `E = 1` 쌍둥이(그림자 포트폴리오)를 추가로
시뮬레이션해야 한다.** 그림자 계열에도 비용·정수 내림·재배분을 동일하게 적용한다
(순환 참조는 없다 — 쌍둥이는 `E = 1` 고정이라 자기 익스포저에 의존하지 않는다).
D는 정의상 `E = 1`. E그룹에도 **σ60 유효(양수) 조건은 그대로 적용**한다(역변동성
가중이 필요하므로).

> **무작위 대조군을 두지 않는다.** 이 전략은 종목을 고르지 않으므로 "무작위 선택"이라는
> 반사실이 성립하지 않는다. 또한 H-010에서 무작위 대조군은 **단일 draw 분산이 재려는
> 효과보다 커서 어느 방향으로도 증거가 되지 못했다**(시드를 바꾸면 부호 반전).
>
> **⚠️ "딱 한 요소만"은 규칙에 대해서만 참이다(사전 인정).** 실현 위험 노출은 상쇄되지
> 않는다 — C는 동일비중이라 고변동성 종목 비중이 A보다 크고, E그룹은 보유 종목이
> 늘며(실측 US 372→500, KR 196→300) **매도 사유 자체가 사라진다**(A의 매도 = 252일
> 수익률 ≤ 0 청산인데 필터를 빼면 청산할 이유가 없다). 따라서 ④는 "절대 모멘텀 필터의
> 기여"보다 **"모멘텀 틸트 vs 유니버스 전체 보유"**를 재는 것에 가깝다.
> H-010 §2.5가 "깨끗한 추정치"라고 썼다가 사후 정정한 실패를 반복하지 않기 위해
> **결과를 보기 전에 인정해 둔다.** 부가 기록으로 그룹별 보유 종목수·실현 변동성을
> 함께 보고해 독자가 confound를 직접 볼 수 있게 한다.
>
> **⚠️ 조건 ③이 실제로 재는 것은 좁다(사전 인정).** 무위험수익률 0 기준 Sharpe는
> **상수 배율에 불변**이므로, E가 상수라면 `Sharpe(A) = Sharpe(D)`가 정확히 성립한다.
> 즉 ③의 신호는 전적으로 **E의 시간 변동(타이밍)**에서만 나온다. 게다가 `E ≤ 1`
> 제약 때문에 A는 익스포저를 줄일 수만 있고 늘릴 수 없다(원 논문은 레버리지를 허용하며
> Sharpe 개선의 상당 부분이 잔잔한 구간의 레버업에서 온다). 그러므로 ③이 묻는 것은
> **"고변동성 구간 뒤에 익스포저를 줄이면 위험조정 성과가 개선되는가"** 한 문장이다.
>
> **⚠️ 변동성 타깃의 구속력 — 시장마다 크게 다르다(사전 기록).** §2.1에서 채택한
> 정의(**OR 마스킹 + 투자일 기준 무스케일 계열**)로 전 구간 실측한 결과:
>
> | | 무스케일 실현변동성 | `E = min(1, 0.12/vol)` |
> |---|---|---|
> | **US** | **12.7%** (타깃 12% 소폭 초과) | 0.945 — 경계선, `E = 1`인 달이 상당수 예상 |
> | **KR** | **20.9%** | **0.575** — 상시 강하게 구속 |
>
> 즉 **KR에서는 처치가 확실히 걸리고, US에서는 약하게 걸린다.** `E = 1`인 달은 A와 D의
> 수익률이 정확히 같아 차분이 0이므로 ③의 유효 표본이 줄어든다 — 실제 `E < 1`인 달 수를
> 부가 기록으로 보고하고, **③의 신뢰구간은 `E < 1`인 달만으로도 따로 산출**한다.
>
> (초안에 적었던 "거의 비구속(US 11.5% / KR 16.3%, E=1인 달 63%·53%)"은 **폐기된
> 정의**(레짐 현금일 포함) 기준이었다. 현금일의 0 수익률이 변동성을 끌어내린 값이라
> 채택 정의와 다르다. 정정 이력을 남긴다.)
>
> 12%는 원 논문의 롱숏 WML(붕괴 시 연 60%+로 치솟는 계열) 기준값이라 롱온리 다종목
> 포트폴리오에는 다소 헐겁다. 논문값을 임의로 바꾸는 것은 튜닝이므로 **12%를 유지하되,
> 타깃 8% 결과를 민감도로 병기**한다(판정 미사용).

## 3. 사전 판정 기준 (결과 보기 전 고정)

포트폴리오 전략이므로 PROTOCOL §3의 "평균·중위 초과수익"을 **월별 초과수익률**
(포트폴리오 월수익률 − 벤치마크 월수익률)로 해석한다.

**채택 조건 — 모두 만족해야 한다**

각 부품의 **목적에 맞는 지표**로 판정한다. 역변동성 가중(②)과 익스포저 조절(③)은
**위험 조절 장치**이므로 위험조정 지표로, 모멘텀 필터(④)는 **수익 장치**이므로
원수익률로 잰다. 이 배정은 결과를 보기 전에 고정했으며, 모든 조건에 대해 두 지표를
**함께 보고**해 사후 재해석의 여지를 없앤다.

| # | 조건 | 판정 지표 | 비용 |
|---|---|---|---|
| ① | A vs B 월초과 **평균·중위 둘 다 양수** | 원수익률 | 비용 후 |
| ② | **ΔSharpe(A − C) > 0** — 역변동성 가중이 동일비중보다 나음 | Sharpe | 비용 후 |
| ③ | **ΔSharpe(A − D) > 0** — 익스포저 조절이 위험조정 성과를 개선 | Sharpe | **gross**(비용 차감 전) |
| ④ | **A − E 월초과 평균 > 0** — 모멘텀 틸트가 전 종목 보유보다 나음 | 원수익률 | 비용 후 |

모든 조건은 **KR·US 양 시장**에서 성립해야 한다. 한 시장만이면 **"부분 지지"**로
기록하고 registry 상태는 `기각`.

**④의 타이브레이커(사전 등록)**: ④는 원수익률로 판정하지만, E그룹도 §2.5에서 인정한
confound(종목수 증가·매도 사유 소멸)를 안고 있다. 지표 배정의 비대칭이 사후 재해석으로
악용되지 않도록 미리 못 박는다 — **④의 ΔSharpe 부호가 원수익률과 반대면, ④는 통과로
세지 않고 "부분 지지 미달"로 기록한다.**

**추가 채택 제한(사전 등록)**: **②(전체 표본 CI) 또는 ③(`E<1` 달 CI) 중 하나라도
신뢰구간이 0을 포함하면 최대 "부분 지지"(= registry `기각`)**로 처리한다. registry의 `채택`은 이후 가설의 중복 판정
근거로 전파되므로, CI가 전부 0을 포함한 채 `채택`이 나오는 경로를 막는다.

**②③의 Sharpe 차이에는 추론을 붙인다(사전 등록).** 점추정만으로는 ΔSharpe = +0.01도
"통과"로 찍힌다. 짝지어진 **월수익률**에 대해 **Jobson–Korkie 검정의 Memmel 보정
통계량**으로 95% 신뢰구간을 산출한다.

```
ΔSR = μ_A/σ_A − μ_D/σ_D                     (월 단위, 무차원)
Var(ΔSR) = (1/T)[ 2 − 2ρ + ½SR_A² + ½SR_D² − SR_A·SR_D·ρ² ]
SE = √Var(ΔSR) ,  CI = ΔSR ± 1.96·SE
   (ρ = corr(r_A, r_D),  SR = μ/σ)
```

> **수식 검증(PROTOCOL §4-(a) 항목 6)**
>
> | 확인 | 결과 |
> |---|---|
> | **차원** | ΔSR은 무차원. 우변 `2 − 2ρ + ½SR² …`도 전부 무차원 → 일치 ✓ |
> | **극단값 ρ = 1, 동일 계열**(A ≡ D, SR_A = SR_D = s) | `2 − 2 + ½s² + ½s² − s²·1 = 0` → **SE = 0** ✓ (같은 계열의 Sharpe 차이는 불확실성이 없다) |
> | **극단값 ρ = 0, 두 Sharpe 모두 0** | `(1/T)[2 − 0 + 0 + 0 − 0] = 2/T` → **SE = √(2/T)** ✓ (독립 두 표본의 알려진 값) |
> | **T 의존성** | `SE ∝ 1/√T` — 표본이 늘면 CI가 좁아진다 ✓ |
>
> **초안의 오류(정정 이력)**: 원래 Jobson–Korkie 원식의 `θ`를 그대로 SE²로 썼는데,
> **θ는 `σ_D·μ_A − σ_A·μ_D`(검정통계량의 분자)의 분산**이지 ΔSR의 분산이 아니다.
> `ΔSR = (σ_D μ_A − σ_A μ_D)/(σ_A σ_D)`이므로 `SE(ΔSR) = √θ/(σ_A σ_D)`가 맞고, 위
> 식은 그것을 정리한 동치 형태다. 차원 확인만 했어도 잡혔을 오류다 — 월수익률
> σ ≈ 0.05에서 `σ_Aσ_D ≈ 0.0025`라 **CI 폭이 실제의 약 1/400**이 되어, 사전 등록한
> "CI가 0을 포함하면 측정 불가" 안전장치가 영영 발동하지 않았을 것이다.

- **월 단위로 계산**한다(연율화하지 않는다 — √12를 곱하면 CI 폭도 같은 배로 커져 결론이
  같으므로, 규약을 하나로 고정한다). 표에는 연율 Sharpe(×√12)와 월 단위 CI를 함께 적고
  단위를 명기한다.
- ③은 **gross 계열**로 검정한다(판정 지표와 동일 계열).
- `E = 1`인 달은 A ≡ D라 짝 상관이 1에 수렴해 CI가 실제보다 좁아진다. 따라서 ③은
  **`E < 1`인 달만으로 산출한 CI를 판정 기준으로 삼고**, 전체 표본 CI는 병기만 한다.
  (아래 "추가 채택 제한"의 ③ 판정도 이 `E<1` CI를 쓴다. 전체 표본 CI는 문서 스스로
  좁다고 인정한 값이라 안전장치의 기준이 될 수 없다. ②는 전체 표본 CI를 쓴다 —
  A와 C는 항상 다른 포트폴리오라 이 축퇴가 없다.)
- ②에 적용할 때는 위 식의 `D`를 `C`로 치환한다.
- **③은 부호도 `E<1` 달 표본으로 판정한다.** 부호는 전체 표본, CI는 `E<1` 달로 나누면
  "E<1 구간 CI가 유의하게 음수인데 전체 표본 부호가 양수라 채택"되는 경로가 열린다
  (Sharpe는 비선형 집계라 부호가 갈릴 수 있다). 판정 표본을 하나로 고정한다.
  전체 표본 ΔSharpe는 병기만 한다. `E<1` 달이 12개월 미만이면 ③은 "표본 부족으로
  측정 불가"로 기록하고 채택 조건을 충족하지 않은 것으로 본다.

아래를 사전 약속한다:

> **CI가 0을 포함하면 해당 조건은 "어느 방향으로도 증거가 아니다"로 §6에 기록한다**
> (부호는 사전 기준대로 판정에 쓰되, 해석문에 "측정 불가"를 명기). H-010이 사후에
> 도달한 결론을 여기서는 사전에 규칙으로 둔다.

**③을 gross로 판정하는 이유**: A는 E가 바뀔 때마다 전 종목을 재조정하므로 D(항상 E=1)
보다 회전율이 구조적으로 높다. H-010 선례와 동일한 처리이며 net 값도 함께 보고한다.

**보고 규칙**
- **조건 ①은 평균과 중위가 둘 다 양수여야 한다**(중위 음수면 탈락, "평균에서만
  성립"으로 통과 처리하지 않는다 — PROTOCOL §3).
- **④는 평균으로 판정**하되 중위와 **음/영/양 달 수**를 함께 보고한다. 레짐 약세로
  전 그룹이 현금인 달은 차분이 정확히 0이라 중위가 축퇴하기 때문이다(H-010에서 관측).
  ③은 `E = 1`인 달에도 A ≡ D라 차분이 0이 되는 **추가 축퇴**가 있으므로 `E < 1`인 달
  수를 함께 보고한다(채택 정의 기준 비율은 미측정 — §2.5의 전 구간 변동성 추정으로는
  KR이 상시 구속·US가 경계로 예상되나 월별 분포는 실행 후에야 안다). 보고 시
  **워밍업 기인분**(투자일 126개 미충족)과 **변동성 기인분**을 분리해 적는다.
- **②③④ 전부에 대해 원수익률 차분과 ΔSharpe를 병기**한다. 판정 지표와 다른 쪽의
  부호가 엇갈리면 §6 판정문에 그 사실을 명기한다.
- 파라미터(252일·60일·126일·연 12%)는 **논문 기본값으로 고정하고 탐색하지 않는다.**
  타깃 8%는 민감도로만 병기한다.

> **다중비교 경계(PROTOCOL §3)** — 4조건 × 2시장 = **8회 검정**이다. 부호만으로
> 판정하는 구조라 우연히 맞는 칸이 나올 수 있으므로, **개별 조건의 부호보다 8칸
> 전체의 패턴**(부호가 시장 간 일관되는가, CI가 0을 벗어나는가)을 §6 해석의 근거로
> 삼는다. 한두 칸만 좋은 결과는 우연으로 간주한다.

> **§6 보고 규칙(사전 등록)** — 조건 ①은 레짐 현금 비중과 시장 베타가 결정하는 값이라
> 가설과 무관한 이유로 탈락할 수 있다(H-009·H-010에서 반복 관측). 이 전략은 변동성
> 타깃까지 있어 익스포저가 구조적으로 100% 미만이므로 **강세장에서는 벤치마크에
> 불리하다.** 따라서 채택 판정은 ①~④ 모두를 요구하되, **§6의 "이 테스트가 실제로
> 답한 것"은 ②③④(부품별 기여)를 기준으로 서술한다.**
>
> **①이 탈락하면 registry 상태는 `기각`이다** — ②③④가 모두 성립해도 마찬가지이며,
> 그 경우 결과 요약에 "부품 기여는 확인됐으나 절대 초과수익은 미달"이라고 적는다.

**부가 기록(판정 미사용)**: CAGR, 연율 변동성, Sharpe, MDD, 연간 회전율(단방향·평균
자본 기준), 평균 투자비중, **그룹별 평균 보유 종목수·실현 변동성**, **월별 익스포저 E의
평균·분포와 `E < 1`인 달 수**, **그룹별 "목표 익스포저 대비 실현 익스포저"**(정수 내림
누수 확인), 레짐 강세 구간 비율, 독립 표본수, 좀비 건수, **④의 t값·95% CI와 음/영/양
달 수**, 타깃 8% 민감도.

## 4. 데이터

| 항목 | KR | US |
|---|---|---|
| 가격 | `cache/prices_kr.parquet` · 646종목 · 기준일 2026-07-21 | `cache/prices_us_test.parquet` · 3,319종목 · 기준일 2026-07-28 |
| 벤치마크 | `^KS11` (마지막 봉 2026-07-22) | `^GSPC` (마지막 봉 2026-07-21) |
| 백테스트 구간 | 워밍업 이후 ~ **`min(가격, 벤치)` = 2026-07-21** | 〃 = **2026-07-21** |
| 난수 시드 | 사용 안 함 (무작위 대조군 없음) | 〃 |

캐시 신선도: 양 시장 모두 6개월 이내 — 갱신 권고 없음.

**워밍업 시작 인덱스**: `max(종목 253봉 충족 시점, 벤치 SMA200, 252)`. 실측 리밸일 수는
US 169개월 · KR 135개월로 H-009·H-010과 같은 구간이다.

**설계 단계에서 실제로 산출한 수치 목록(사전 등록 투명성)** — 아래는 게이트 검토
과정에서 **결과를 보기 전에** 측정한 값이며, §2·§3의 규칙을 정하는 근거로 썼다.
**그룹 간 성과 비교(A vs B/C/D/E)는 일절 산출하지 않았다.**

| 항목 | US | KR |
|---|---|---|
| 적격 종목수 (평균 / 최소 / 최대) | 372 / 184 / 490 | 196 / 107 / 298 |
| 1주 미달 종목수 (평균) | 0 | 6.9 |
| 정수 내림 후 실제 배분율 (`E=1` / `E=0.5`) | 99.98% / — | 91.0% / 83.4% |
| 무스케일 실현변동성(채택 정의) | 12.7% | 20.9% |
| 레짐 강세 리밸일 비율 | 84.6% | 58.5% |
| A 평균 투자비중(추정) | 82.3% | 51.7% |
| 리밸일 수 | 169 | 135 |

이 중 **A 평균 투자비중**은 조건 ①이 강세장에서 벤치마크에 구조적으로 불리함을
뜻한다. H-009(KR 50.7% → ① 탈락)·H-010(57.5% → 중위 탈락)의 연장선이며, §3의 보고
규칙은 이 구조를 알고 세운 것이다.

---

## 5. 결과

### 5.1 KR — 2015-05-29 ~ 2026-07-21 (독립 표본 134개월)

| 그룹 | 월초과 평균 | 월초과 중위 | CAGR | 변동성 | Sharpe | MDD | 회전율/년 | 투자비중 | 종목수 |
|---|---|---|---|---|---|---|---|---|---|
| **A** 전체 | **−0.523%p** | **−0.057%p** | 6.89% | **9.6%** | **0.74** | **−16.9%** | 2.1x | 44.6% | 110 |
| **B** 벤치마크 | 0 | 0 | **10.97%** | 24.9% | 0.54 | −43.9% | — | 100% | — |
| **C** 동일비중 | −0.495%p | −0.232%p | 7.21% | 10.0% | 0.74 | −18.3% | 1.8x | 40.6% | 111 |
| **D** 타깃없음 | −0.194%p | +0.339%p | 10.24% | 16.6% | 0.67 | −28.3% | 2.5x | 58.3% | 113 |
| **E** 필터없음 | −0.691%p | −0.313%p | 4.79% | 9.4% | 0.54 | −21.6% | 1.9x | 46.6% | 167 |

| 조건 | 값 | 95% CI | 표본 | 판정 |
|---|---|---|---|---|
| ② ΔSharpe(A−C) 비용후 | −0.0007 (월) | [−0.0340, +0.0326] | 134 | ✗ · **CI 0 포함** |
| ③ ΔSharpe(A−D) gross (E<1 달) | +0.0565 | [−0.0410, +0.1539] | 61 | 부호 ✓ · **CI 0 포함** |
| 　(전체 표본 병기) | +0.0263 | [−0.0387, +0.0914] | 134 | — |
| ④ A−E 월초과 평균 | **+0.168%p** | [+0.024, +0.312] · t=2.28 | 134 | ✓ · **CI 0 제외** |
| 　ΔSharpe(A−E) 타이브레이커 | +0.0575 | — | — | 부호 일치 |

음/영/양 달: A−C 46/46/42 · A−E 34/42/58

### 5.2 US — 2012-07-31 ~ 2026-07-21 (독립 표본 168개월)

| 그룹 | 월초과 평균 | 월초과 중위 | CAGR | 변동성 | Sharpe | MDD | 회전율/년 | 투자비중 | 종목수 |
|---|---|---|---|---|---|---|---|---|---|
| **A** 전체 | **−0.326%p** | **+0.019%p** | 9.18% | 9.4% | 0.98 | −17.7% | 2.1x | 78.8% | 315 |
| **B** 벤치마크 | 0 | 0 | **12.89%** | 14.1% | 0.93 | −33.9% | — | 100% | — |
| **C** 동일비중 | −0.339%p | −0.042%p | 8.99% | 9.7% | 0.94 | −19.7% | 1.8x | 72.2% | 315 |
| **D** 타깃없음 | −0.239%p | +0.094%p | 10.24% | 10.2% | **1.01** | −21.4% | 2.3x | 84.5% | 315 |
| **E** 필터없음 | −0.389%p | −0.045%p | 8.36% | 9.5% | 0.89 | −21.1% | 1.5x | 78.2% | 423 |

| 조건 | 값 | 95% CI | 표본 | 판정 |
|---|---|---|---|---|
| ② ΔSharpe(A−C) 비용후 | +0.0133 (월) | [−0.0244, +0.0511] | 168 | 부호 ✓ · **CI 0 포함** |
| ③ ΔSharpe(A−D) gross (E<1 달) | +0.0020 | [−0.0377, +0.0416] | 71 | 부호 ✓ · **CI 0 포함** |
| 　(전체 표본 병기) | −0.0089 | [−0.0305, +0.0127] | 168 | — |
| ④ A−E 월초과 평균 | +0.063%p | [−0.019, +0.144] · t=1.51 | 168 | 부호 ✓ · **CI 0 포함** |
| 　ΔSharpe(A−E) 타이브레이커 | +0.0264 | — | — | 부호 일치 |

음/영/양 달: A−C 76/15/77 · A−E 70/15/83

> **CI를 산출한 6칸(②③④ × 2시장) 중 0을 벗어난 것은 KR ④ 하나뿐이다**(①은 CI를
> 산출하지 않았다). §3의 다중비교 경계("한두 칸만 좋은 결과는 우연")를 그대로
> 적용해야 한다.
>
> **③의 원수익률 병기(§3 요구)**: A−D 월별 차분은 net 기준 KR −0.329%p · US −0.086%p로
> **양 시장 모두 음수**다. 판정 지표(ΔSharpe)와 부호가 엇갈린다 — §6에서 다룬다.

### 5.3 민감도 (§2.3·§3 사전 등록, 판정 미사용)

| | KR | US |
|---|---|---|
| 본안 (당일 종가 체결·타깃 12%) | 평균 −0.523%p · CAGR 6.89% · Sharpe 0.74 | 평균 −0.326%p · CAGR 9.18% · Sharpe 0.98 |
| **익일 시가 체결** | 평균 −0.543%p · CAGR 6.64% · Sharpe 0.72 | 평균 −0.345%p · CAGR 8.93% · Sharpe 0.95 |
| **타깃 8%** | 평균 −0.749%p · CAGR 4.29% · Sharpe 0.69 · E 평균 0.554 | 평균 −0.513%p · CAGR 6.99% · **Sharpe 1.02** · E 평균 0.663 |

체결 가정을 바꿔도 결론은 불변이다. 타깃을 8%로 조이면 **KR은 Sharpe까지 나빠지고
US는 소폭 좋아진다**(0.98 → 1.02) — 방향이 시장마다 갈려 일관된 개선이 아니다.

### 5.4 부가 기록

| 항목 | KR | US |
|---|---|---|
| **강세 리밸일 적격 종목수** (평균/최소/최대) | 195 / 107 / 296 (상한 300) | 372 / 184 / 490 (상한 500) |
| 익스포저 E (평균/중위) | 0.772 / 0.780 | 0.920 / 0.980 |
| E<1 리밸 / 워밍업 E=1 / 저변동성 E=1 | 112 / 13 / 10 (합 135) | 93 / 7 / 69 (합 169) |
| **목표 대비 실현 익스포저** (A/C/D/E) | 99.9 / 100.0 / 99.7 / 99.9 % | 100.0 / 100.0 / 100.0 / 100.0 % |
| 목표비중 대비 실현비중 평균절대편차 | 3.2 / 3.1 / 2.7 / 3.5 bp | 0.0 bp (전 그룹) |
| 레짐 강세 거래일 비율 | 58.5% | 84.0% |
| 좀비(리밸일 발생 건수 누적) | 58건 | 0건 |

- **적격 종목수가 사전 등록값(§4)과 사실상 일치**한다 — US 372/184/490은 정확히 같고,
  KR은 195/107/296 vs 등록 196/107/298로 미세하게 다르다(설계 단계 실측은 전 리밸일
  기준, 위 표는 **강세 리밸일** 기준이라 모집단이 다르다). 규격대로 구현됐음을
  확인하는 지표이며, 가설이 주장한 "종목 수가 시장 상태에 따라 변한다"도 실제로
  관측된다(KR 107~296, US 184~490).
- **목표 대비 실현 익스포저가 99.7~100%** — §2.3-5의 잔여 현금 재배분이 의도대로 작동해
  정수 내림 누수(설계 단계 실측 KR 9~17%)가 사실상 제거됐다.
- **E 강도가 설계 예상과 다르다**(설계 추정 KR 0.575 → 실측 0.772). 원인은 §5.5 참조.

### 5.5 검증과 수정

`--selftest` 5개 항목 통과: ① **Memmel CI의 극단값을 코드로 재확인**(동일 계열 → SE=0,
독립 → SE=√(2/T)) ② 투자일 리밸→리밸 블록 라벨링 ③ 익스포저 계산(고변동성 → E<1,
저변동성 → E=1) ④ 단일 종목 매매 규칙 ⑤ **다종목 시뮬레이터를 느린 참조 구현과
대조해 최종 자본 1e-6 이내 일치**

PROTOCOL §4-(b) 코드 검토가 11건을 지적했고, **결과를 바꾸는 5건을 고쳐 양 시장을
재실행**했다.

| # | 지적 | 조치 |
|---|---|---|
| **P1** | **유동성 유니버스와 모멘텀 필터의 적용 순서가 §2.2와 반대** — 모멘텀을 먼저 걸고 그 안에서 상위 N을 잘라, **US 적격 종목이 항상 500(상한)으로 고정**됐다. §1이 주장한 "가변 breadth"가 소멸하고, `A ⊆ E`도 깨져 ④가 재는 대상이 달라졌다 | §2.2 순서(유동성 → 적격)로 정정. **사전 등록값 재현으로 확인**(US 372/184/490, KR 195/107/296) |
| P2 | ③의 "E<1 달" 표본에 **약세장 전액 현금 달**이 섞여 실효 표본이 2배로 부풀려짐 (수정 전 진단: KR 104개 중 50개가 차분 정확히 0) | `E<1 ∧ 실제 투자` 조건으로 좁힘 (KR 61 · US 71) |
| P3 | **사전 등록한 민감도 2건(타깃 8%·익일 시가)이 아예 산출되지 않음** | 둘 다 구현해 §5.3에 보고 |
| P5 | "실현/목표 익스포저"로 라벨링된 값이 실제로는 실현 익스포저 | 목표 대비 비율로 정정 (99.7~100%) |
| P6 | E 워밍업 분해 출력이 자기모순(13+105≠105) | 세 범주가 합계와 일치하도록 정정 |

> **P1 수정이 결과에 미친 영향** — KR ①의 중위가 +0.077 → **−0.057%p로 부호 반전**,
> KR ④의 t값이 3.58 → 2.28, **US ④가 t=2.14 → 1.51로 유의성을 잃었다**(CI가 0을 포함).
> 최종 판정은 바뀌지 않지만 해석의 강도가 크게 달라진다.

나머지 6건(P4 배당 비대칭, P7 채택 제한 미출력, P8 적격 0일 때 미청산, P9 좀비 정의,
P10 벤치 결측일, P11 경고 출력)은 §7 한계와 출력 문구로 처리했다. P7은 스크립트가
`▸ 추가 채택 제한` 행을 직접 찍도록 추가했다.

---

## 6. 판정

### **기각** (부분 지지에도 미달)

| # | 조건 | KR | US | 결과 |
|---|---|---|---|---|
| ① | A vs B 평균·중위 둘 다 양수 | ✗ (−0.523 / −0.057) | ✗ (−0.326 / **+0.019**) | **미충족** |
| ② | ΔSharpe(A−C) > 0 | ✗ (−0.0007) | 부호 ✓ (+0.0133) | **미충족** (양 시장 불일치 + 양쪽 CI 0 포함) |
| ③ | ΔSharpe(A−D) > 0, E<1 달 | 부호 ✓ (+0.0565) | 부호 ✓ (+0.0020) | **CI 0 포함** |
| ④ | A−E 평균 > 0 | ✓ (+0.168, t=2.28) | 부호 ✓ (+0.063, t=1.51) | KR만 유의 |

**§3의 추가 채택 제한이 발동한다** — ②(전체 표본 CI)와 ③(`E<1` 달 CI)이 **양 시장
모두 0을 포함**하므로 최대 "부분 지지"이며 registry 상태는 `기각`이다. ①도 양 시장
탈락이므로 어느 경로로 보나 결론은 같다.

### 이 테스트가 실제로 답한 것 (§3 사전 등록 규칙에 따라 ②③④ 기준)

**세 부품 중 근거가 남은 것은 절대 모멘텀 필터 하나뿐이고, 그마저 한 시장에서만이다.**

- **②(역변동성 가중)**: 부호가 시장마다 뒤집히고(KR −0.0007, US +0.0133) 양쪽 CI가
  0을 크게 포함한다. **동일비중과 구분되지 않는다.**
- **③(익스포저 조절)**: 부호는 양 시장 양수지만 CI가 0을 포함한다. KR은 +0.0565로
  값이 제법 크나 CI [−0.041, +0.154]로 넓다. **측정 불가**다.
  **더구나 §3이 병기를 요구한 원수익률 차분(A−D)은 양 시장 모두 음수다**
  (net KR −0.329%p · US −0.086%p). 즉 판정 지표(Sharpe)와 병기 지표(원수익률)의
  **부호가 엇갈린다** — 익스포저를 줄이면 위험조정 지표는 소폭 나아지지만 수익은
  줄어든다는 뜻이며, 이는 변동성 타깃의 정의상 예상된 방향이다.
- **④(절대 모멘텀 필터)**: **KR에서만 유의**하다(+0.168%p, 중위 +0.000%p, CI가 0을
  벗어남). US는 +0.063%p·중위 +0.000%p·t=1.51로 **CI가 0을 포함하므로 "측정 불가"**다.
  CI를 산출한 6칸(②③④ × 2시장) 중 유일하게 0을 벗어난 칸이며, §3의 다중비교
  경계상 **"한 칸만 좋은 결과는 우연"**에 해당한다.

### 가장 중요한 해석 — Sharpe 우위는 알파가 아니라 저노출이다

A의 Sharpe가 양 시장 모두 벤치마크를 웃돈다(KR 0.74 vs 0.54, US 0.98 vs 0.93).
MDD도 크게 낫다(−16.9% vs −43.9%, −17.7% vs −33.9%). 그런데 ①은 탈락했다.
**이 조합은 모순이 아니라 "현금을 섞어 위험을 줄인 것"의 정의 그대로다.**

- 무위험수익률 0 기준 Sharpe는 **배율에 불변**이다. A는 평균 44.6%(KR)/78.8%(US)만
  투자하므로 위험도 수익도 함께 줄어 Sharpe는 유지·소폭 개선되지만 **CAGR은 벤치마크에
  크게 미달**한다(KR 6.89 vs 10.97, US 9.18 vs 12.89).
- 회귀로 확인하면 **베타가 KR 0.234 / US 0.436**이고, 월별 초과수익의 **부호가 벤치
  상승월과 하락월에서 반대이며 크기가 비슷하다** — KR 상승월(75개) 평균 −3.59%p ·
  하락월(59개) +3.38%p, US 상승월(114개) −1.22%p · 하락월(54개) +1.55%p. 알파가 아니라
  베타 축소 그 자체다.
- 같은 Memmel 검정을 A−B에 적용하면 **KR +0.0589 CI[−0.093, +0.211], US +0.0137
  CI[−0.117, +0.144]로 둘 다 0을 포함**한다. ②③에 적용한 잣대를 그대로 쓰면
  **Sharpe 우위도 벤치마크와 통계적으로 구분되지 않는다.**
- §2.2가 `E ≤ 1`로 레버리지를 금지했으므로 이 Sharpe 우위를 수익으로 바꿀 방법도 없다.

따라서 **"위험조정 성과가 좋다"는 서술은 "현금을 섞어 위험을 줄였다"의 재진술 이상이
아니다.** 덧붙여 US는 개별주만 배당이 반영되고 `^GSPC`는 가격지수라(§7-2), 배당을
맞추면 벤치마크 Sharpe가 A를 앞선다.

### H-009·H-010과 합쳐 읽으면

| 테스트 | 물은 것 | 답 |
|---|---|---|
| H-009 | 모멘텀을 **어떻게 재는가**(회귀 vs 누적수익률) | 중요하지 않다 |
| H-010 | 모멘텀 위에 **경로 필터**를 얹으면 | 효과 없음(측정 불가) |
| **H-012** | **고르지 말고 얼마나 담을지**로 승부하면 | 위험은 줄지만 초과수익은 없다 |

세 테스트가 서로 다른 축에서 같은 곳에 도달했다. **종목 선정 규칙에서도, 사이징
규칙에서도 벤치마크를 이길 근거를 찾지 못했다.** 유일하게 반복 관측되는 것은
"모멘텀 상위를 고르는 행위 자체는 무작위·전 종목 보유보다 낫다"(H-009 ④, H-012 ④)
뿐이며, 그 크기는 시장에 따라 유의하지 않을 만큼 작다.

## 7. 한계

| # | 한계 | 영향 |
|---|---|---|
| 1 | **생존편향이 비대칭이다** — 유니버스가 현재 상장 스냅샷이라 A·C·D·E에만 걸리고 벤치마크 B에는 걸리지 않는다 | A~E의 절대 성과가 낙관적. 그럼에도 ① 탈락이므로 결론은 보수적인 방향 |
| 2 | **US는 전략만 배당 포함, 벤치마크는 배당 제외** — 개별주는 `auto_adjust=True`로 배당 재투자 반영, `^GSPC`는 가격지수 | 연 ~1.8%p 전략 유리. **배당을 맞추면 B의 Sharpe가 0.93 → 약 1.06으로 A(0.98)를 앞선다.** US의 Sharpe 우위는 이것으로 설명될 수 있다. KR은 pykrx 수정주가(현금배당 미조정) + `^KS11` 가격지수라 대체로 대칭 |
| 3 | **8칸 중 CI가 0을 벗어난 것은 1칸(KR ④)뿐** | 다중비교 경계상 우연으로 볼 수 있다. 어떤 조건도 "확인됐다"고 말할 수 없다 |
| 4 | **③의 유효 표본이 작다** — E<1이면서 실제 투자한 달이 KR 61 · US 71개월 | Memmel 점근 CI의 신뢰도가 떨어진다 |
| 5 | **설계 단계 E 추정이 빗나갔다** — 예상 KR 0.575 → 실측 0.772. 원인은 "전 구간 통합 변동성 점추정"과 "리밸일별 롤링 후 평균"의 차이(Jensen 부등식 + `min(·,1)` 상한 절단) | §2.5의 "KR은 상시 강하게 구속"은 과장이었다. 실제 처치는 예상보다 약했다 |
| 6 | **C의 익스포저가 A보다 구조적으로 낮다**(강세 리밸 평균 C 0.683 vs A 0.752, 전 리밸 0.700 vs 0.772) — 동일비중이 고변동성 종목을 그대로 담아 무스케일 변동성이 높고, 타깃이 더 강하게 구속하기 때문 | ② 오염은 아니다(§2.5가 "각 그룹은 자기 무스케일 계열로 E를 계산"으로 사전 등록). 다만 CAGR 차이를 "가중 방식의 우열"로 오해하면 안 된다 |
| 7 | **좀비 58건은 리밸일 발생 건수 누적**이라 H-010의 3건과 직접 비교할 수 없다. 증가는 `open ≤ 0` 마스킹이 KR 거래정지 봉 9,008개를 잡아낸 결과(방어 강화)다 | 규모는 2만 포지션-리밸 중 0.35%. 좀비는 예산·정규화에서 제외돼 실현 익스포저를 아주 소폭 과소평가 |
| 8 | **레버리지 금지(`E ≤ 1`)** — 원 논문은 레버업을 허용하며 Sharpe 개선의 상당 부분이 거기서 나온다 | 논문 세팅을 그대로 재현한 것이 아니다. ③이 잴 수 있는 것은 "줄이는 쪽" 절반뿐 |
| 9 | **적격 0종목이면 전량 청산**하도록 구현했다(§2.3-3(a) 해석). 실측상 강세 리밸에서 적격 0회는 양 시장 0회 | 결과 영향 없음 |
| 10 | **당일 종가 체결 가정** — §5.3 익일 시가 체결로 확인했고 결론 불변 | KR·US 모두 부호·판정 유지 |
| 11 | **벤치마크 결측 거래일** — `bull_map.get(d, False)`라 벤치 데이터가 없는 날은 약세로 처리된다(KR 4일) | **리밸일 중 벤치 결측은 양 시장 0건**이라 매매에는 영향이 없고, "레짐 강세 비율" 표기만 미세하게 낮아진다 |
| 12 | **§6·§7의 진단 수치(베타·A−B ΔSharpe·상승/하락월 초과·C 익스포저)는 스크립트가 출력하지 않는다** | 문서 검토에서 현행 코드로 재산출해 갱신했다. 재현하려면 §8의 방법을 따라야 한다 |

---

## 8. 재현 방법

저장소 루트에서 실행한다. 로컬 캐시만 사용하며 재다운로드하지 않는다.

```bash
# 코드 자체 검증 (캐시·네트워크 불필요, 수 초)
PYTHONPATH=.:docs/analysis .venv/bin/python \
  docs/analysis/backtests/scripts/tsmom_vol_target.py --selftest

# 시장별 백테스트 (민감도 2종·부가 기록 포함)
PYTHONPATH=.:docs/analysis .venv/bin/python -W ignore \
  docs/analysis/backtests/scripts/tsmom_vol_target.py kr
PYTHONPATH=.:docs/analysis .venv/bin/python -W ignore \
  docs/analysis/backtests/scripts/tsmom_vol_target.py us
```

- 난수 시드 없음(무작위 대조군을 두지 않았다 — §2.5).
- 데이터 기준일 KR 2026-07-21 · US 2026-07-28. **백테스트 종료일은 양 시장 모두
  2026-07-21**(벤치마크 마지막 봉으로 절단 — §4).
- §5.1~§5.4의 모든 수치가 위 두 명령의 표준 출력에 그대로 나온다.
- 공용 하네스: `btlib.loading`(캐시 로딩) · `btlib.regime`(레짐) · `btlib.costs`(비용
  원천값). 시뮬레이터는 `btlib.engine`이 단일 거래 평가기이고 H-009·H-010의 것도
  이 가설의 매매 절차(가변 종목수·역변동성 가중·동적 익스포저·잔여 현금 재배분)와
  달라 새로 작성했으며, §5.5의 자체 검증으로 확인했다.
- **§6·§7의 진단 수치**(베타, A−B ΔSharpe, 벤치 상승/하락월 초과수익, 그룹별 익스포저,
  A−D 원수익률 차분, 배당 보정)는 위 명령의 표준 출력에 없다. `run_market(market,
  quiet=True)`가 `(rows, out, exposures, P, bull, a_eq, bench_eq)`를 반환하므로 이를
  받아 산출한다:
  - 베타·상승/하락월 초과: `monthly(out[("A",True)]["equity"])`와 `monthly(bench_eq)`의
    회귀 및 벤치 부호별 평균
  - A−B ΔSharpe: `delta_sharpe_ci(월수익률_A, 월수익률_B)`
  - 그룹별 익스포저: `exposures[g]`의 평균(강세 리밸일만 또는 전 리밸일)
  - 배당 보정: 벤치 월수익률에 연 1.8%p(월 0.15%p)를 더한 뒤 Sharpe 재계산
    → B 0.935 → **1.063**으로 A(0.982)를 앞선다
